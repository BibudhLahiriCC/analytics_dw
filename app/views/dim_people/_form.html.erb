<%= form_for(@dim_person) do |f| %>
  <% if @dim_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dim_person.errors.count, "error") %> prohibited this dim_person from being saved:</h2>

      <ul>
      <% @dim_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
